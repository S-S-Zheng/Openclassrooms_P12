# CI vérif auto code est propre, passe les tests et est maintenable avant CD
# S'éxé à chaque push/PR sans déploy, secrets et modif d'env externes
---
name: CI Pipeline

# Déclencheurs
# Lance le workflow à chaque push sur main, develop et à chaque pull request
# vers main si PR change.
on:
  push:
    branches:
      - main
      # - develop
  pull_request:
    branches:
      - main
  #   types: [synchronize, opened]

# Variables d'environnement globales
env:
  PYTHON_VERSION: "3.12"
  POETRY_VERSION: "2.2.1"

# Jobs
jobs:
  lint:
    name: Linting
    runs-on: ubuntu-latest
    # étapes du lint
    steps:
      # Clone le dépôt dans le runner
      - name: Checkout code
        uses: actions/checkout@v4

      # Configure python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      # Installe les outils de lint
      - name: Install lint tool Ruff
        run: |
          python -m pip install --upgrade pip
          pip install ruff

      # Erreurs affichées dans l'onglet "Annotations" du PR GitHub
      - name: Run Ruff Check (Linter + Imports)
        run: ruff check src/ tests/ --output-format=github

      - name: Run Ruff Format (Style)
        run: ruff format src/ tests/ --check

  tests:
    name: Pytests & Smoke Test
    runs-on: ubuntu-latest
    # Oblige la validation de lint avant
    needs: lint
    services:
      # service lance un conteneur docker séparé
      # postgre attend nécéssairement les variables nommés ci-dessous
      postgres:
        image: postgres:15
        env:
          # Variables préfixées par postrgres: POSTGRES_
          POSTGRES_USER: ${{ vars.POSTGRES_USER_TEST }}
          POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD_TEST }}
          POSTGRES_DB: ${{ vars.POSTGRES_DB_TEST }}
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          lfs: true # Pour récupérer les fichiers lourds et pas des pointeurs

      - name: Install Poetry
        run: pipx install poetry==${{ env.POETRY_VERSION }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: "poetry"
      # Installe les dépendances
      - name: Install dependencies
        run: poetry install --with dev

      # Smoke test
      - name: Model Smoke Test (Inference Check)
        run: |
          poetry run python smoke_test.py
        # Ce test vérifie que le modèle ONNX est chargeable AVANT de lancer les tests longs

      # Lance les tests avec coverage
      - name: Run Pytests with coverage
        env:
          # POSTGRES_USER: ${{ vars.POSTGRES_USER_TEST }}
          # POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD_TEST }}
          # POSTGRES_DB: ${{ vars.POSTGRES_DB_TEST }}
          # DATABASE_URL: postgresql://$POSTGRES_USER:$POSTGRES_PASSWORD@localhost:5432/$POSTGRES_DB #noqa: E501
          # On force Settings à regarder le Postgres local du Runner
          DB_HOST: localhost
          DB_PORT: 5432
          DB_USER: ${{ vars.POSTGRES_USER_TEST }}
          SB_PASSWORD: ${{ secrets.POSTGRES_PASSWORD_TEST }}
          DB_NAME: ${{ vars.POSTGRES_DB_TEST }}
          DATABASE_URL: postgresql://$DB_USER:$SB_PASSWORD@$DB_HOST:$DB_PORT/$DB_NAME
          MISTRAL_API_KEY: "dummy_key_for_ci"
          HUGGINGFACE_TOKEN: "dummy_token_for_ci"
        run: |
          poetry run python -m pytest --cov=livrable_p12 --cov-config=coverage.ini --cov-report=xml

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.xml
      # - name: Archive coverage report
      #   uses: actions/upload-artifact@v4
      #   # Garde le rapport même si un test échoue
      #   if: always()
      #   with:
      #     name: coverage-report
      #     path: htmlcov/
